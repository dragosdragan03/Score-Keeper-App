import 'package:flutter/material.dart';

class BridgeGame extends StatelessWidget {
  const BridgeGame({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Bridge Game')),
    );
  }
}
