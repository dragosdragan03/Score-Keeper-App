import 'package:flutter/material.dart';

class WhistGame extends StatelessWidget {
  const WhistGame({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Whist Game')),
    );
  }
}
